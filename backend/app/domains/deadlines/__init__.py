"""Deadlines domain package.

Routes for deadlines and action items — mirrors
`src/app/api/deadlines/**/route.ts` and
`src/app/api/action-items/**/route.ts`. Both belong to the same domain
since action items are deadline-adjacent task tracking.
"""
