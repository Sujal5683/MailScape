"""Misc domain package.

Routes for saved searches (filter presets) and email templates (compose
presets) — mirrors `src/app/api/saved-searches/**/route.ts` and
`src/app/api/templates/**/route.ts`.
"""
