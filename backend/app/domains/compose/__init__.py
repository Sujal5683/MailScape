"""Compose domain package.

Routes for saving drafts, sending emails (confirmation-gated), and
recipient autocomplete — mirrors `src/app/api/compose/**/route.ts` and
`src/app/api/recipients/search/route.ts`.
"""
